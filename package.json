{
  "private": "true",
  "name": "budget-exchange-2",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.8.3"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
    "scripts": {
    "dev": "concurrently 'yarn workspace backend run dev' 'yarn workspace frontend run dev' 'pg_ctl -D \"$PGDATA\" -l logfile start'"
  }
}
